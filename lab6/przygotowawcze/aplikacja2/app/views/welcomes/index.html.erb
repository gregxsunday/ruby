<h1>Welcomes#index</h1>
<p>Find me in app/views/welcomes/index.html.erb</p>
<%
=begin%>
 <%= p Welcome.first.message %><br>
<%= p Welcome.second.message %><br>
<%= p Welcome.last.message %> 
<%
=end%>

<!-- Treść formularza -->
<%= form_with url: welcomes_index_path, method: :get, local: true do |form| %>
  <!-- Tutaj umieść wywołanie pomocnika 'collection_select()' lub 'select()' -->
  <%= select("welcome", "category_id", Category.all.collect {|p| [ p.name, p.id ] }, { include_blank: true }) %>
  <%= form.submit %>
<% end %>